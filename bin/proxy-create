#!/bin/env sh
ID=$(docker run --name $1   \
       -v /tmp:/tmp \
       -v /home/$USER:/home/$USER \
       -e USERID=$UID:$GID      \
       -it -d $2 \
       /bin/bash)

echo $ID > "$(dirname $0)/../proxy.d/$1"
proxy-select $1
