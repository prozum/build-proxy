#!/usr/bin/env sh
PROXY=$(cat "$(dirname $0)/../proxy.d/current")
PROFILE="$(dirname $0)/../profile.d/current"
docker exec -i \
            -u=$UID:$(id -g $USER) \
            -w=$(pwd) \
            $(cat $PROFILE/profile.env) \
            $PROXY \
            "$1" \
            "${@:2}"
